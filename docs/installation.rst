============
Installation
============

At the command line::

    $ easy_install django-tvml

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-tvml
    $ pip install django-tvml
